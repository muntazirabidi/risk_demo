# Railway/Nixpacks configuration
# This helps Railway detect Node.js in the backend directory

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npm run build || true"]

[start]
cmd = "cd backend && npm start"

